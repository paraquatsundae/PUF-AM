{
  "name": "@pufam/mist-freenet",
  "private": true,
  "version": "0.1.0",
  "description": "PUF-AM mist unit — Freenet-style encrypted farm storage (phase 3: FCP adapter)",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config ../../vitest.config.ts units/mist-freenet"
  }
}
