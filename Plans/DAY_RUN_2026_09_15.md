# Day run — 2026-09-15

**Status:** Active plan — today's checklist  
**Date:** 2026-09-15  
**Product:** PUF-AM  
**Scope:** Leftover **0.0.2** GitHub drafts, then codebase health, a focused security review, chill portions weather/DPIRD (parallel after 1–2), then issue photos over Freenet (approach only — do not implement in the agent that wrote this file).  
**Experimental — not production.** Firebase Auth + invite PIN remains the shipping path. Freenet / mist stays behind the workshop bake.

George's order. Do **not** mix the chill pack job with Freenet photos. Do **not** enable `freenet.service`. Do **not** wipe `~/.config/PUF-AM`. Do **not** delete Clare Downs. Do **not** renumber [`SETTINGS_SYNC_AND_CREW.md`](SETTINGS_SYNC_AND_CREW.md) or any `Plans/reference/*` § headings.

Index: [`README.md`](README.md). Naming procedures: [`NAMING.md`](NAMING.md) §9.

---

## Leftover state (facts, 2026-09-15 morning AWST)

Lid-close overnight (2026-09-14 evening → 2026-09-15 morning) did **not** leave a dirty tree. A conversation-start `git status` snapshot that listed those files as uncommitted is stale — they are in `e280abc`.

| Fact | Value |
|------|--------|
| Branch | `master`, **up to date** with `origin/master` |
| Working tree | **Clean** (0 dirty paths) |
| HEAD | `e280abcc1cf113ab2242b0a06228c2003d20bb03` — *Ship workshop bake 0.0.2 for the three-device Freenet Bones paddock-sync milestone.* |
| Commit time | 2026-09-14 21:47:40 +0800 (`ParaquatSundae`) |
| `package.json` | `version` **0.0.2**, `androidVersionCode` **2** — consistent; Gradle reads both ([`NAMING.md`](NAMING.md) §2) |
| In-app version | `APP_VERSION` from `VITE_APP_VERSION` (`src/brand.ts`); Vite bakes `package.json` `version` |
| Local AppImage | `release/PUF-AM-0.0.2.AppImage` (21:45, gitignored) — also leftover `PUF-AM-0.0.1.AppImage` (21:35, the live-run binary) |
| Local debug APK | `android/app/build/outputs/apk/debug/app-debug.apk` (21:47, ~29 MB, gitignored) |
| Secrets | Still uncommitted (**good**): `secrets/`, `.env`, `android/local.properties`, `firebase-applet-config.json` |
| `libfreenet.so` | `android/app/src/main/jniLibs/` gitignored (**good** — do not commit) |
| `Plans/FREENET_OPERATOR_FLOW.md` | Already has **Milestone — 2026-09-14** (header + §8) and **Next (2026-09-15)** (header + §8). This file is that Next. |
| Git tags on `origin` | `apk-3`, `apk-4` only — **no** `v0.0.2` |
| GitHub Releases | Latest **published** is still [`apk-4`](https://github.com/paraquatsundae/PUF-AM/releases/tag/apk-4). Two **drafts**, both titled *0.0.2 — three-device Freenet Bones paddock sync*, both `tag_name` `v0.0.2`, both `target_commitish` `e280abc`, both unpublished: |

| Draft (created UTC) | Assets | URL |
|---------------------|--------|-----|
| 2026-09-14 13:48:22Z (21:48 AWST, ~1 min after the commit) | none | https://github.com/paraquatsundae/PUF-AM/releases/tag/untagged-b8d0be50fb5b31393f6e |
| 2026-09-14 21:28:23Z (05:28 AWST 15th — this morning) | `app-debug.apk` 29 031 876 B uploaded | https://github.com/paraquatsundae/PUF-AM/releases/tag/untagged-ca4d06d011335944a33f |

Neither draft has an AppImage. Neither is published. `gh release list` shows both as Draft.

**Verdict:** git / bump / local APK / local AppImage / docs **landed**. Public GitHub ship is **half-finished** (two drafts, APK on one, no AppImage, no tag). Prefer documenting that gap over publishing, uploading, or deleting from this pass.

---

## 0. Unjam 0.0.2

- [x] Docs already record the 2026-09-14 milestone and the 2026-09-15 next (operator flow §8, this file).
- [x] `package.json` 0.0.2 / `androidVersionCode` 2 — not an inconsistent half-bump.
- [x] Commit + push on `origin/master` (`e280abc`).
- [x] Local AppImage + debug APK exist under gitignored `release/` and `android/app/build/`.
- [ ] **GitHub leftover (do not finish unless George asks):** two `v0.0.2` drafts. Decide later: keep one draft, attach `PUF-AM-0.0.2.AppImage`, drop the empty duplicate, then publish — **or** leave unpublished (workshop bake; public **v 0.1** is still a future `0.1.0`). Do not bump again. Do not commit secrets or `jniLibs/*.so`.

---

## 1. Codebase health

[`CODEBASE_HEALTH.md`](CODEBASE_HEALTH.md) — layering (page → hook → lib), file-size soft **400** / hard **600**, SoC greps. Gate: `npm run audit:codebase`. Procedure A (packs/nav/modules/grants): `npm test && npm run lint && npm run plugins:verify && npm run audit:codebase` ([`CODEBASE_HEALTH.md`](CODEBASE_HEALTH.md) § Debug and audit procedure).

- [x] `npm run audit:codebase` **passed** 2026-09-15. No code change. Freenet cards WARN only (`MistWorkshopCard` 1095, `MistFarmSyncCard` 1003). Layering / SoC / cycles / `harvest_drying` / pack folders OK. In-scope at the 600 cap: `mapStore.ts` 600.
- Prepended to [`logs/CODEBASE_HEALTH_CHECK.md`](logs/CODEBASE_HEALTH_CHECK.md); dated line in [`CODEBASE_HEALTH.md`](CODEBASE_HEALTH.md) size appendix.
- Do not enable `strict`, merge map hooks, remount `MapContainer`, or grow `KNOWN_OVERSIZE` Freenet cards without a split.

---

## 2. Security check (focused — not App Check)

- [x] **2026-09-15:** Security review — diff docs-only; landed tree `e280abc` Freenet/mist + API-key checklist green. No medium+ findings. Residuals already documented: Android loopback WS, hybrid FarmCode boundary, workshop `npm run dev` hub.

He asked for a security check **before** deeper Freenet photos. Scope: **Freenet/mist + last night's landed tree** (`e280abc` / `cd26d09`), against [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) and [`FIREBASE_BILLING.md`](FIREBASE_BILLING.md). Not a drive-by App Check (deferred **2026-09-07**, [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) § App Check).

Confirm, do not “improve”:

| Rule | Cite | Check |
|------|------|--------|
| DPIRD is server-only | [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) § DPIRD; [`NAMING.md`](NAMING.md) §3 | `.env` has `DPIRD_API_KEY`; **never** `VITE_DPIRD_API_KEY` (Vite bakes `VITE_*` into APK/bundle) |
| BYO key stays off George's kit | [`FIREBASE_BILLING.md`](FIREBASE_BILLING.md) §3; `functions-byo-weather/` | Not in Firestore, not in the client, not in George's Secret Manager |
| BYO no-endpoint fails closed | [`FIREBASE_BILLING.md`](FIREBASE_BILLING.md); `tests/byoWeatherRouting.test.ts` | Never fall back to `am.pufworks.farm` / George's weather cache |
| Refuse-list | [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) § Hosted home; `src/lib/byoFirebaseConfig.ts`; `functions-byo-weather/src/constants.ts`; `scripts/refuseHostedProject.mjs` | Reject **both** `gen-lang-client-0444791425` and `pufworks-am` |
| Tile proxy stays unauthenticated | [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) § Map imagery | `/api/tiles/:z/:x/:y` — Leaflet `<img>` cannot send a header |
| No App Check | [`API_KEY_SECURITY.md`](API_KEY_SECURITY.md) § App Check | Do not wire it as a drive-by |
| Freenet / mist | [`FREENET_OPERATOR_FLOW.md`](FREENET_OPERATOR_FLOW.md) §9; [`FREENET_NETWORK_PACK.md`](FREENET_NETWORK_PACK.md) Decision — 2026-09-12 | Ciphertext before PUT; FarmSeed paper/owner-only (not on a `PUF-` ticket); host never holds farm keys; attach only Freenet 0.2; do not kill `org.freenet.androidnode`; do not re-enable `freenet.service` |

Also: no `VITE_GOOGLE_MAPS_API_KEY`; hosted farms stay on Firestore `(default)` on `pufworks-am`.

---

## 3. Chill portions plugin (parallel after 1–2)

**Not** the Freenet photos job. Start at [`PLUGIN_AUTHORING.md`](PLUGIN_AUTHORING.md) § Template pack — chill portions. Copy [`plugins/chill_portions/`](../plugins/chill_portions/), **not** walnut blight ([`NAMING.md`](NAMING.md) §1 — crop pack ≠ Freenet plugin). Contract: [`CROP_PACK_PLUGIN.md`](CROP_PACK_PLUGIN.md). Freshness design (do not invent a new cache): [`DPIRD_CACHE_FRESHNESS.md`](DPIRD_CACHE_FRESHNESS.md).

**Why today:** weather reference is stale; the standalone chill system (`PUFworks-chill_calculator/`) and its DPIRD connections changed (key handling included).

| Piece | Where |
|-------|--------|
| Catalog + engine | `plugins/chill_portions/plugin.json`, `engine.json` |
| UI | `plugins/chill_portions/src/` |
| Farm hourly (DPIRD) | `shared/weather/chillPortions.ts` — server `GET /api/weather/chill-portions` (`server/chillRoutes.ts`) cannot import from the pack folder |
| Daily / CSV calculator | `shared/weather/chillCalculator.ts` (no API key) |

**Hard (same as §2):** never `VITE_DPIRD_API_KEY`; BYO key not in Firestore / client / George's SM; BYO with no weather endpoint fails closed.

- [x] **2026-09-15:** Weather reference aligned with the current proxy / BYO / Cloud Run path. Pack UI already used `apiUrl`/`apiFetch` (no `VITE_DPIRD_API_KEY`). Docs + science copy + tests updated. Residual: `functions-byo-weather` still has no `GET /api/weather/chill-portions` ([`FIREBASE_BILLING.md`](FIREBASE_BILLING.md) §3.3 / §7).

Do not hot-load React from a zip. Do not share `settings/model_params`. Verify with `npm run plugins:verify -- plugins/chill_portions`.

---

## 4. Issue photos over Freenet — after 1+2 only

**Do not implement in this day-run write-up.** Do not claim photos are shipped. Hosted Storage path (≤5 compressed JPEGs) already landed in `cd26d09`; Freenet helpers exist (`src/mist/mistPhotoFreenet.ts`, `src/lib/photoCompress.ts`) but Opennet photo transfer is **not** field-validated.

Decisions already on disk ([`FREENET_NETWORK_PACK.md`](FREENET_NETWORK_PACK.md) Decision — 2026-09-14; [`FREENET_OPERATOR_FLOW.md`](FREENET_OPERATOR_FLOW.md) §9.4; [`SETTINGS_SYNC_AND_CREW.md`](SETTINGS_SYNC_AND_CREW.md) Decision — 2026-09-14 photos — **do not renumber**):

| Constraint | Value |
|------------|--------|
| Max photos | **5** per issue (`MAX_PHOTOS_PER_RECORD`) |
| Longest edge | **1600 px** |
| JPEG quality | **0.72 → 0.40** (step down); reject if still over |
| Hard cap after compress | **600 KB** (`PHOTO_HARD_CAP_BYTES`) |
| Seal | **HotKey** only — never FarmSeed |
| Keys | `mist/v1/farm/{id}/hot/photo/{issueId}/{photoId}` (legacy first: `…/hot/photo/{issueId}`); diary/events `…/hot/photo/event/{eventId}/{photoId}` |
| Watch | 20 s ping grows `photoIndexUri` / `photoIndexHash`; GET **only** new hashes. Do **not** republish Hot on every photo. |
| Splitfile | **Deferred** (frozen: no splitfiles for KiB-class Hot/bones; photos are not KiB-class) |

**The hurdle (packet size):** workshop pack PUT asserts `FREENET02_MAX_BLOB_BYTES` = **64 KiB** (`units/mist-freenet/src/freenet02-pack-id.ts` — “splitfiles not supported”). A 600 KB JPEG cannot take that path. Desktop IPC ceiling is **8 MiB** (`FREENET_IPC_MAX_BLOB_BYTES`) — that is a runaway guard, not a Freenet budget.

Approach when 1+2 are green (choose then, do not pick in this file):

1. Measure a real paddock JPEG through `compressFarmPhoto` vs 64 KiB.
2. Options: prove 0.2.135 will take a larger single blob and raise **our** assert; chunk without calling it splitfile (new contract); crush quality/edge until it fits (likely unusable); or keep Freenet as **index + miss-warn** and leave bytes on-device / hosted until splitfile is a real project.
3. Do not re-enable the Firestore outbox on Freenet-native farms. Crew never need FarmSeed.

---

## Honest residuals (still true)

From [`FREENET_OPERATOR_FLOW.md`](FREENET_OPERATOR_FLOW.md) §8 plus yesterday's devices:

- First Opennet can take **minutes** (Send/Join wait for N≥1, 120 s PUT window).
- Freenet Android Node (`org.freenet.androidnode`) **cannot** be force-stopped from this uid — Settings says so and offers **Open that app**.
- Hosted web (`am.pufworks.farm`) **has no node** — install desktop or the APK.
- FarmSeed is **paper-only** on owner devices — do not print Clare Downs' FarmCode; do not put FarmSeed on a short ticket.
- Phone **wireless adb** listen port changes after wireless debugging restarts — `adb connect <ip>:<port>` again (`scripts/adb-install-debug.mjs`); USB is the stable path.
- Hole 5 **advanced, not closed** — two Android devices still need to exchange with **no** Freenet Android Node. Official `libfreenet.so` still missing from GitHub.
- Do not re-enable `freenet.service`. Do not wipe `~/.config/PUF-AM`. Do not delete Clare Downs.

---

## What this day-run is not

- Not a public **v 0.1** (`0.1.0`) cut.
- Not a Cloud Run / `am.pufworks.farm` deploy unless George asks after 1–2.
- Not an App Check rollout.
- Not finishing the GitHub 0.0.2 drafts from here.
